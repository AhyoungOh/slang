import { DropdownButton, Dropdown } from 'react-bootstrap';
import 'react-dropdown/style.css';

const option = ['slang1', 'slang2', 'slang3'];

function List() {
  return <Dropdown placeholderClassName='myPlaceholderClassName'></Dropdown>;
}

export default List;
